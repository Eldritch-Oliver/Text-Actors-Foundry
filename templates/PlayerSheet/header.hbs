<header class="sheet-header">
	<img
		src="{{actor.img}}"
		data-action="editImage"
		title="{{actor.name}}"
		height="64"
		width="64"
	/>
	<input
		type="text"
		name="name"
		class="large"
		value="{{actor.name}}"
		placeholder="{{ localize 'Name' }}"
	/>
</header>
